# Copy this file to .env and fill in your own key. .env is git-ignored and
# must never be committed. Get a free Gemini API key from Google AI Studio:
# https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=

# Non-secret tunables (confidence threshold, retrieval top_k, LLM model, etc.)
# live in config.yaml, not here.
